# Changelog

## Unreleased

- Added the SH7 fail-closed standalone-admission audit, run-bound CI platform
  evidence, installed-tarball CycloneDX validation and a privacy-safe external-
  consumer evidence policy. Publication and the CLI bridge remain blocked.
- Added SH6 Graph adoption conformance across public contracts, compatibility,
  validation and registry subpaths without advancing Graph maturity.
- Added explicit browser/Node surfaces and the bounded `workspai-shared`
  validation executable with installed-package and payload-disclosure gates.

- Added the SH3-C deterministic scale corpus and executable latency, observed-
  heap, soak, worker-concurrency, cold-load, cancellation, extension and
  adversarial gates, with machine-readable local baseline and explicit remote
  limitations.
- Added immutable hard validation ceilings and cooperative resource-traversal
  cancellation without introducing Node dependencies into the runtime API.

- Added the SH3-B compatibility candidate with a frozen previous envelope,
  forward-only deterministic negotiation, explicit migration/loss metadata,
  opaque extension preservation and packed-consumer/adversarial coverage.

- Consolidated architecture, governance and contributor documentation in the
  canonical Workspai documentation portfolio. The public package retains only
  root consumer metadata; tarball leak checks enforce the boundary.

- Added the SH3-A multi-contract generator candidate: manifest-driven isolated
  types and standalone validators, declared external-reference resolution,
  cycle/identity/output rejection, atomic generation, a portfolio digest and a
  self-validating immutable contract catalog with generic lookup/validation.

- Closed the SH2 stabilization boundary with schema-generated vocabulary,
  fail-closed organization-scope admission, honest diagnostic truncation and an
  installed-tarball consumer smoke.

- Added unpublished Shared protocol-foundation scaffold.
- Added draft WIS-shaped result types and immutable contract helper.
- Added publication guards, package documentation and conformance placeholders.
- Added the source-reviewed reference landscape, CLI contract inventory and
  candidate scale/extension profile for SH0.
- Added machine-readable maturity gates and a redacted CLI bridgeability fixture
  manifest without connecting Shared to the released CLI runtime.
- Added CI-enforced dependency, evidence and discovery-mirror drift guards.
- Prepared WIS RFC-0001/RFC-0002 and Shared binding ADRs for semantic review.
- Separated result status from operation outcome and aligned the private
  TypeScript scaffold with the SH1 candidate vocabulary.
- Added strict deterministic JSON Schema-to-TypeScript and standalone ESM
  validator generation with immutable schema-digest registry and drift checks.
- Added bounded resource, structural and semantic validation plus candidate
  conformance fixtures and gzip bundle budgets.
